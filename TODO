# $Id$
- liblog.sh: не проходит тест udfWarn без терминала (в не интерактивном режиме)
+ libini.sh: при тесте с другими модулями в ini файлах оказывается вывод других
  тестовых блоков (решено в версии 1.57)
+ libXXX.sh: во всех функциях с записью результата в идентификатор переменной
  требуется создание уникальных имен локальных переменных!
- libini.sh: udfGetIniSection не работает если имя конфигурационного файла
  заканчивается на "."
- создать опции командной строки, которые позволят манипулировать режимом работы
  сценария: журналирование и т.п.
- . bashlyk args
- fhs ++
- {xml,md5} -> std
- subproc for test throw & log func
- job test udfOnTrap (libstd)
- use csv functions from libini for libcnf
- include lines from test unit to test log
- phenomen $s udfGetConfig




